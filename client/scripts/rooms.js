var Rooms = {
  // $appendRooms: function () {
  //   let getData = async () => {
  //     var result = await App.fetch();
  //     return result;
  //   };
  //   let data = getData();
  //   console.log('this is data', data);
  //   for (let i = 0; i < data.results.length; i++) {
  //     if ($('select #options').find(data.results[i]) === undefined) {
  //       let $option = '<option value="' + data.results[i] + '">' + data.results[i] + '</option>';
  //       $($option).appendTo('select #options');
  //     }

  //   }

  // }

};